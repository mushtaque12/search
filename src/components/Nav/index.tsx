import { NavContainer, NavItem } from './styles';

const Nav = () => (
  <NavContainer>
    <NavItem to="/">Home</NavItem>
    <NavItem to="/products">Products</NavItem>
  </NavContainer>
);

export default Nav;
