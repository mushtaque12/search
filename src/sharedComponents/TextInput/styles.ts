import styled from 'styled-components';

export const TextField = styled.div`
  width: 100%;
  > label {
    padding: .5em;
    }
  > input {
  padding: .5em;
  border: 0;
  width: 100%;
  outline: none;
  }
`;